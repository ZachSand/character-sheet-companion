export interface ActorOverview {
  id: string;
  name: string;
  armorClass: number;
  currentHealth: number;
  maxHealth: number;
  proficiencyBonus: number;
  initiativeBonus: number;
  mainClass: string;
  overallLevel: number;
  imageData: string;
}
